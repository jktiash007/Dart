// variables
void main() {
  print("hello world tulut tata");
  // var name = "tiash";
  // print(name);
  //name = 22;
  //print(name);
  String name1 = "piash";
  print(name1); 
  dynamic name = "tiash";
  print(name);
  int num = 22;
  double float = 12.22;
  print(float);
  print(num);
  bool tel = true;
  print(tel);
  tel = false;
  print(tel);
 print(num*float);
 print(name+" "+name1);
 name =2;
 final oo=32;
 const yy= "yu";
 print(oo);
 print(yy);


  /*
if a var variable is assigned with string , it can not me used as int or number later. for that we need dynamic type variable.
can not use same name for different type of variable.
debug starts from main func.
for print use print keyword.
value of double or float can not be stored in int type variable.
value of int type can be store in double type variable.
if we dont want to change a value in a variable we have to use final and const keyword as variable type.


  */
}